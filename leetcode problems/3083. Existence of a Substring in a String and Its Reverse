
class Solution {
public:
    bool isSubstringPresent(string s) {
        string ss=s;
        reverse(ss.begin(),ss.end());
        for(int i=0;i<ss.length()-1;i++){
            if(ss.find(s.substr(i,2))!=-1) return true;
            
        }
        return false;
    }
};
