int equalizeArray(vector<int> arr) {
    unordered_map<int,int> ans;
    int max=0;
    for (int i=0;i<arr.size();i++) {
        ans[arr[i]]++;
         if (ans[arr[i]] > max) {
            max = ans[arr[i]];
        }
    }
    return arr.size()-max;
}
