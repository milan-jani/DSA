string catAndMouse(int x, int y, int z) {
    int a = abs(z - x);
    int b = abs(z - y); 

    if (a == b) {
        return "Mouse C"; 
    } else if (a < b) {
        return "Cat A";
    } else {
        return "Cat B"; 
    }
}
