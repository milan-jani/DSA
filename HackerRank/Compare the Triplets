vector<int> compareTriplets(vector<int> a, vector<int> b) {
    vector<int> c(2);
    c[0]=0;
    c[1]=0;
    for(int i=0;i<a.size();i++){
        if(a[i]>b[i]){
            c[0]++;
        }
         if(a[i]<b[i]){
            c[1]++;
        }
    }
    return c;
    
}
